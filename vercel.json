{
  "buildCommand": "pnpm build",
  "installCommand": "npm install -g pnpm@10.11.1 && pnpm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}